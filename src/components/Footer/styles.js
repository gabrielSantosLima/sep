import styled from 'styled-components';

export const Container = styled.footer`
    background: var(--color-footer);
    padding: 4px;
    text-align: center;
    letter-spacing: 3px;
    color: white;
    font-size: 7pt;
`;
